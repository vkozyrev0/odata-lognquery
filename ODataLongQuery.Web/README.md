# OData 202 demo client (Angular 22)

Browser client for the OData long-running query sample. `ng serve` proxies `/odata` and `/async` to `http://localhost:5268`.

See the repo root [README](../README.md).

```bash
dotnet run --project ../ODataLongQuery
npm start
```

Then open http://localhost:4200.
